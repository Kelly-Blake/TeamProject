//globalvariables
var currentScene = 1;
var currentColor = color(0, 0, 0);
/*var color1 = color(255, 255, 0);
var color2 = color(255, 255, 255);
var color3 = color(43, 171, 60);*/
var coloredsquares = 0;

//Alyssa bitmoji
var drawHead = function(bitmojiX,bitmojiY,bitmojiSize) {
    var p = bitmojiSize/100;
    //face
    fill(232, 213, 157);
    noStroke();
    rect(bitmojiX+91*p,bitmojiY+115*p,21*p,28*p);
    ellipse(bitmojiX+100*p,bitmojiY+83*p,99*p,100*p);
    //eyes
    fill(255, 255, 255); //whites
    ellipse(bitmojiX+84*p,bitmojiY+76*p,20*p,20*p);
    fill(255, 255, 255);
    ellipse(bitmojiX+120*p,bitmojiY+76*p,20*p,20*p);
    fill(138, 74, 5); //brown
    noStroke();
    ellipse(bitmojiX+120*p,bitmojiY+79*p,13*p,14*p);
    fill(138, 74, 5);
    ellipse(bitmojiX+84*p,bitmojiY+79*p,13*p,14*p);
    fill(0, 0, 0); //pupil
    ellipse(bitmojiX+120*p,bitmojiY+80*p,7*p,9*p);
    fill(0, 0, 0);
    ellipse(bitmojiX+84*p,bitmojiY+80*p,7*p,9*p);
    fill(255, 255, 255); //shine
    noStroke();
    ellipse(bitmojiX+122*p,bitmojiY+80*p,5*p,2*p);
    fill(255, 255, 255);
    ellipse(bitmojiX+86*p,bitmojiY+80*p,5*p,2*p);
    //mouth
    fill(232, 213, 157);
    stroke(0, 0, 0);
    arc(bitmojiX+101*p,bitmojiY+105*p,20*p,12*p,10,170);
    //nose
    line (bitmojiX+102*p,bitmojiY+86*p,bitmojiX+98*p,bitmojiY+97*p);
    line (bitmojiX+105*p,bitmojiY+97*p,bitmojiX+97*p,bitmojiY+97*p);
    //brows
    fill(232, 213, 157);
    stroke(0, 0, 0);
    arc(bitmojiX+120*p,bitmojiY+66*p,20*p,12*p,200,340);
    fill(232, 213, 157);
    stroke(0, 0, 0);
    arc(bitmojiX+85*p,bitmojiY+66*p,20*p,12*p,200,340);
    var p = bitmojiSize/100;
    //hair
    fill (79, 49, 14);
    noStroke();
    quad(bitmojiX+62*p,bitmojiY+137*p,bitmojiX+38*p,bitmojiY+143*p,bitmojiX+55*p,bitmojiY+40*p,bitmojiX+75*p,bitmojiY+50*p);
    quad(bitmojiX+131*p,bitmojiY+58*p,bitmojiX+144*p,bitmojiY+137*p,bitmojiX+163*p,bitmojiY+146*p,bitmojiX+149*p,bitmojiY+42*p);
    fill(0, 0, 0);//beanie
    arc (bitmojiX+102*p,bitmojiY+54*p,102*p,69*p,169,374);
    
};

var drawBody = function(bitmojiX,bitmojiY,bitmojiSize) {
    var p = bitmojiSize/100;
    //shirt
    fill(64, 67, 128);
    noStroke();
    quad(bitmojiX+82*p,bitmojiY+250*p,bitmojiX+39*p,bitmojiY+250*p,bitmojiX+50*p,bitmojiY+147*p ,bitmojiX+92*p,bitmojiY+137*p);
    quad(bitmojiX+129*p,bitmojiY+250*p,bitmojiX+164*p,bitmojiY+250*p,bitmojiX+155*p,bitmojiY+147*p,bitmojiX+110*p,bitmojiY+137*p);
    rect(bitmojiX+87*p,bitmojiY+138*p,30*p,113*p);
    rect(bitmojiX+79*p,bitmojiY+147*p,54*p,104*p);
    fill(154, 153, 199);
    textSize(29*p);
    text("AK",bitmojiX+83*p,bitmojiY+183*p);
};


var drawBitmoji = function(bitmojiX,bitmojiY,bitmojiSize) {
    drawHead(bitmojiX,bitmojiY,bitmojiSize);
    drawBody(bitmojiX,bitmojiY,bitmojiSize);
};

//Kelly bitmoji
var drawKHead = function(bitmojiX,bitmojiY,bitmojiH) 
{
noStroke();

//Head and Hair
var t = bitmojiH/100;

fill(255, 219, 172);//Skin Color
ellipse(bitmojiX+(-460*t),bitmojiY+(135*t),93*t,100*t);//head


fill(69, 48, 48);//top hair color
ellipse(bitmojiX+(t*-460),bitmojiY+(t*86),t*87,t*43); //top hair 

fill(255, 219, 172);//skin Color for forhead
ellipse(bitmojiX+(t*-460),bitmojiY+(t*115),t*58,t*-51);//forhead

fill(69, 48, 48);//Hair color
quad(bitmojiX+(t*-513),bitmojiY+(t*84),bitmojiX+(t*-454),bitmojiY+(t*63),bitmojiX+(t*-502),bitmojiY+(t*153),bitmojiX+(t*-517),bitmojiY+(t*246)); // Left hair 
quad(bitmojiX+(t*-415),bitmojiY+(t*227),bitmojiX+(t*-394),bitmojiY+(t*96),bitmojiX+(t*-408),bitmojiY+(t*85),bitmojiX+(t*-438),bitmojiY+(t*58)); // Left hair 

//Eyes
fill(250, 250, 250);//eye fill 
ellipse(bitmojiX+(t*-440),bitmojiY+(t*124),t*18,t*21); //right eye
ellipse(bitmojiX+(t*-480),bitmojiY+(t*124),t*18,t*21); //left eye

fill(120,134,108);// eye middle color
ellipse(bitmojiX+(t*-440),bitmojiY+(t*124),t*9,t*12); //right eye-middle
ellipse(bitmojiX+(t*-480),bitmojiY+(t*124),t*9,t*12); //left eye-middle

fill(0, 0, 0);
ellipse(bitmojiX+(t*223),bitmojiY+(t*124),t*4,t*5); //right eye-middle
ellipse(bitmojiX+(t*193),bitmojiY+(t*124),t*4,t*5); //left eye-middle

//Nose
stroke(0, 0, 0);//nose outline
strokeWeight(1);
fill(255, 219, 172);//skin color for nose
bezier(bitmojiX+(t*-460), bitmojiY+(t*149),bitmojiX+(t*-466), bitmojiY+(t*147), bitmojiX+(t*-467), bitmojiY+(t*159), bitmojiX+(t*-468), bitmojiY+(t*136));//nose shape 

//mouth
stroke(235, 91, 91);
fill(255, 255, 255);//teeth color
arc(bitmojiX+(t*-458),bitmojiY+(t*161),t*34,t*25,353,545);//mouth 
line(bitmojiX+(t*-477),bitmojiY+(t*160),bitmojiX+(t*-443),bitmojiY+(t*160));//Top of mouth

//Hat
noStroke();
fill(245, 159, 242);//hat color
ellipse(bitmojiX+(t*-454),bitmojiY+(t*46),t*83,t*39);//hat lip
ellipse(bitmojiX+(t*-451),bitmojiY+(t*80),t*137,t*69);//hat top
};

var drawKBody = function (bitmojiX,bitmojiY,bitmojiH) 
{
var t = bitmojiH/100;
//Shirt
noStroke();
fill(0, 0, 0);//Long sleeve color
rect(bitmojiX+(t*-476),bitmojiY+(t*181),t*41,t*38);//turtle neck 
rect(bitmojiX+(t*-504),bitmojiY+(t*207),t*14,t*78);//Arm L 
rect(bitmojiX+(t*-411),bitmojiY+(t*207),t*14,t*78);//Arm R

fill(90, 130, 21);//shortsleeve color
rect(bitmojiX+(t*-489),bitmojiY+(t*189),t*76,t*94);//shortsleeve base
ellipse(bitmojiX+(t*-489),bitmojiY+(t*204),t*36,t*30);//shoulders L
ellipse(bitmojiX+(t*-413),bitmojiY+(t*204),t*36,t*30);//shoulders R

//Shirt design 
fill(0, 0, 0);
rect(bitmojiX+(t*-435),bitmojiY+(t*203),t*7,t*27);//Cactus design 
arc(bitmojiX+(t*-433),bitmojiY+(t*217),t*20,t*16,435,567);//Cactus arm L
arc(bitmojiX+(t*-428),bitmojiY+(t*207),t*20,t*17,317,486);//Cactus arm R

//Initials  (K)
fill(255, 255, 255);
rect(bitmojiX+(t*-482),bitmojiY+(t*236),t*4,t*28);//Inital K 
triangle(bitmojiX+(t*-484),bitmojiY+(t*244),bitmojiX+(t*-458),bitmojiY+(t*234),bitmojiX+(t*-476),bitmojiY+(t*251));//Upper K 
triangle(bitmojiX+(t*-486),bitmojiY+(t*270),bitmojiX+(t*-458),bitmojiY+(t*264),bitmojiX+(t*-476),bitmojiY+(t*248));//Lower K

//Initials (B)
stroke(255, 255, 255);//line B
strokeWeight(4);// Thinker line B
noFill();
ellipse(bitmojiX+(t*-432),bitmojiY+(t*246),t*18,t*12);//Upper B
ellipse(bitmojiX+(t*-432),bitmojiY+(t*261),t*17,t*13);//Lower B
rect(bitmojiX+(t*-447),bitmojiY+(t*240),t*2,t*29);//Inital B 
strokeWeight(1);
};

var drawKelly = function(bitmojiX,bitmojiY,bitmojiH)
{
    drawKHead (bitmojiX,bitmojiY,bitmojiH);
    drawKBody (bitmojiX,bitmojiY,bitmojiH);
};

//squares
var Square = function(config) {
    this.x = config.x || 176;
    this.y = config.y || 50;
    this.width = config.width || 40;
    this.height = config.height || 40;
    this.label = config.label || "1";
    this.color = config.color;
    this.onClick = config.onClick || function() {};
};

Square.prototype.draw = function() {
    fill(this.color);
    rect(this.x, this.y, this.width, this.height);
    fill(0, 0, 0);
    textSize(15);
    text(this.label, this.x+16, this.y+this.height/4);
};

Square.prototype.isMouseInside = function() {
    return mouseX > this.x &&
           mouseX < (this.x + this.width) &&
           mouseY > this.y &&
           mouseY < (this.y + this.height);
};

Square.prototype.handleMouseClick = function() {
    if (this.isMouseInside()) {
        this.onClick();
    }
};

//yellow 1 squares
var coloredcolor1Square1 = new Square({ //the filled in square is drawn on top of the empty square
    x: 175,
    y: 150,
    width: 42,
    label: " ",
    color: color(255, 255, 0),
});

var color1Square1 = new Square({ //the square with no color and number visable
    x: 175,
    y: 150,
    width: 42,
    label: "1",
    color: color(209, 209, 209),
    onClick: function() {
        if (currentColor === color(255, 255, 0)) {
        draw = function() {
            noStroke();
           coloredcolor1Square1.draw(); 
        };
        }
    }
    
});

//white 2 squares
var coloredcolor2Square1 = new Square({
    x: 217,
    y: 150,
    width: 42,
    label: " ",
    color: color(255, 255, 255),
});

var color2Square1 = new Square({
    x: 217,
    y: 150,
    width: 42,
    label: "2",
    color: color(230, 230, 230),
    onClick: function() {
        if (currentColor === color(255, 255, 255)) {
        draw = function() {
            noStroke();
           coloredcolor2Square1.draw();
        };
        }
    }
});

var coloredcolor2Square2 = new Square({
    x: 133,
    y: 150,
    width: 42,
    label: " ",
    color: color(255, 255, 255),
});

var color2Square2 = new Square({
    x: 133,
    y: 150,
    width: 42,
    label: "2",
    color: color(230, 230, 230),
    onClick: function() {
        if (currentColor === color(255, 255, 255)) {
        draw = function() {
            noStroke();
           coloredcolor2Square2.draw();
        };
        }
    }
});

var coloredcolor2Square3 = new Square({
    x: 175,
    y: 110,
    width: 42,
    label: " ",
    color: color(255, 255, 255),
});

var color2Square3 = new Square({
    x: 175,
    y: 110,
    width: 42,
    label: "2",
    color: color(230, 230, 230),
    onClick: function() {
        if (currentColor === color(255, 255, 255)) {
        draw = function() {
            noStroke();
           coloredcolor2Square3.draw();
        };
        }
    }
});

var coloredcolor2Square4 = new Square({
    x: 175,
    y: 190,
    width: 42,
    label: " ",
    color: color(255, 255, 255),
});

var color2Square4 = new Square({
    x: 175,
    y: 190,
    width: 42,
    label: "2",
    color: color(230, 230, 230),
    onClick: function() {
        if (currentColor === color(255, 255, 255)) {
        draw = function() {
            noStroke();
           coloredcolor2Square4.draw();
        };
        }
    }
});

var coloredcolor3Square1 = new Square({
    x: 175,
    y: 230,
    width: 42,
    label: " ",
    color: color(43, 171, 60),
});

//green 3 squares
var color3Square1 = new Square({
    x: 175,
    y: 230,
    width: 42,
    label: "3",
    color: color(186, 186, 186),
    onClick: function() {
        if (currentColor === color(43, 171, 60)) {
        draw = function() {
            noStroke();
           coloredcolor3Square1.draw();
        };
        }
    }
});

var coloredcolor3Square3 = new Square({
    x: 175,
    y: 270,
    width: 42,
    label: " ",
    color: color(43, 171, 60),
});

var color3Square3 = new Square({
    x: 175,
    y: 270,
    width: 42,
    label: "3",
    color: color(186, 186, 186),
    onClick: function() {
        if (currentColor === color(43, 171, 60)) {
        draw = function() {
            noStroke();
           coloredcolor3Square3.draw();
        };
        }
    }
});

var coloredcolor3Square2 = new Square({
    x: 217,
    y: 230,
    width: 42,
    label: " ",
    color: color(43, 171, 60),
});

var color3Square2 = new Square({
    x: 217,
    y: 230,
    width: 42,
    label: "3",
    color: color(186, 186, 186),
    onClick: function() {
        if (currentColor === color(43, 171, 60)) {
        draw = function() {
            noStroke();
           coloredcolor3Square2.draw();
        };
        }
        
    }
});

//scene 2 squares

//light pink squares
var scene2coloredlightpinkSquare1 = new Square({ //the filled in square is drawn on top of the empty square
    x: 175,
    y: 150,
    width: 42,
    label: " ",
    color: color(255, 97, 202),
});

var scene2lightpinkSquare1 = new Square({ //the square with no color and number visable
    x: 175,
    y: 150,
    width: 42,
    label: "3",
    color: color(209, 209, 209),
    onClick: function() {
        if (currentColor === color(255, 97, 202)) {
        draw = function() {
            noStroke();
           scene2coloredlightpinkSquare1.draw(); 
        };
        }
    }
    
});

//pink squares
var scene2coloredpinkSquare1 = new Square({
    x: 133,
    y: 150,
    width: 42,
    label: " ",
    color: color(255, 18, 144),
});

var scene2pinkSquare1 = new Square({
    x: 133,
    y: 150,
    width: 42,
    label: "2",
    color: color(230, 230, 230),
    onClick: function() {
        if (currentColor === color(255, 18, 144)) {
        draw = function() {
            noStroke();
           scene2coloredpinkSquare1.draw(); 
        };
        }
    }
});

var scene2coloredpinkSquare2 = new Square({
    x: 217,
    y: 150,
    width: 42,
    label: " ",
    color: color(255, 18, 144),
});

var scene2pinkSquare2 = new Square({
    x: 217,
    y: 150,
    width: 42,
    label: "2",
    color: color(230, 230, 230),
    onClick: function() {
        if (currentColor === color(255, 18, 144)) {
        draw = function() {
            noStroke();
           scene2coloredpinkSquare2.draw(); 
        };
        }
    }
    
});

var scene2coloredpinkSquare3 = new Square({
    x: 217,
    y: 190,
    width: 42,
    label: " ",
    color: color(255, 18, 144),
});

var scene2pinkSquare3 = new Square({
    x: 217,
    y: 190,
    width: 42,
    label: "2",
    color: color(230, 230, 230),
    onClick: function() {
        if (currentColor === color(255, 18, 144)) {
        draw = function() {
            noStroke();
           scene2coloredpinkSquare3.draw(); 
        };
        }
    }
});

var scene2coloredpinkSquare4 = new Square({
    x: 133,
    y: 190,
    width: 42,
    label: " ",
    color: color(255, 18, 144),
});

var scene2pinkSquare4 = new Square({
    x: 133,
    y: 190,
    width: 42,
    label: "2",
    color: color(230, 230, 230),
    onClick: function() {
        if (currentColor === color(255, 18, 144)) {
        draw = function() {
            noStroke();
           scene2coloredpinkSquare4.draw(); 
        };
        }
    }
});

var scene2coloredpinkSquare5 = new Square({
    x: 175,
    y: 190,
    width: 42,
    label: " ",
    color: color(255, 18, 144),
});

var scene2pinkSquare5 = new Square({
    x: 175,
    y: 190,
    width: 42,
    label: "2",
    color: color(230, 230, 230),
    onClick: function() {
        if (currentColor === color(255, 18, 144)) {
        draw = function() {
            noStroke();
           scene2coloredpinkSquare5.draw(); 
        };
        }
    }
});

var scene2coloredpinkSquare6 = new Square({
    x: 175,
    y: 230,
    width: 42,
    label: " ",
    color: color(255, 18, 144),
});

var scene2pinkSquare6 = new Square({
    x: 175,
    y: 230,
    width: 42,
    label: "2",
    color: color(230, 230, 230),
    onClick: function() {
        if (currentColor === color(255, 18, 144)) {
        draw = function() {
            noStroke();
           scene2coloredpinkSquare6.draw(); 
        };
        }
    }
});

//red squares
var scene2coloredredSquare1 = new Square({
    x: 133,
    y: 110,
    width: 42,
    label: " ",
    color: color(255, 0, 0),
});

var scene2redSquare1 = new Square({
    x: 133,
    y: 110,
    width: 42,
    label: "1",
    color: color(186, 186, 186),
    onClick: function() {
        if (currentColor === color(255, 0, 0)) {
        draw = function() {
            noStroke();
           scene2coloredredSquare1.draw(); 
        };
        }
    }
});

var scene2coloredredSquare2 = new Square({
    x: 217,
    y: 110,
    width: 42,
    label: " ",
    color: color(255, 0, 0),
});

var scene2redSquare2 = new Square({
    x: 217,
    y: 110,
    width: 42,
    label: "1",
    color: color(186, 186, 186),
    onClick: function() {
        if (currentColor === color(255, 0, 0)) {
        draw = function() {
            noStroke();
           scene2coloredredSquare2.draw(); 
        };
        }
    }
    
});

var scene2coloredredSquare3 = new Square({
    x: 91,
    y: 150,
    width: 42,
    label: " ",
    color: color(255, 0, 0),
});

var scene2redSquare3 = new Square({
    x: 91,
    y: 150,
    width: 42,
    label: "1",
    color: color(186, 186, 186),
    onClick: function() {
        if (currentColor === color(255, 0, 0)) {
        draw = function() {
            noStroke();
           scene2coloredredSquare3.draw(); 
        };
        }
    }
    
});

var scene2coloredredSquare4 = new Square({
    x: 91,
    y: 190,
    width: 42,
    label: " ",
    color: color(255, 0, 0),
});

var scene2redSquare4 = new Square({
    x: 91,
    y: 190,
    width: 42,
    label: "1",
    color: color(186, 186, 186),
    onClick: function() {
        if (currentColor === color(255, 0, 0)) {
        draw = function() {
            noStroke();
           scene2coloredredSquare4.draw(); 
        };
        }
    }
    
});

var scene2coloredredSquare5 = new Square({
    x: 259,
    y: 150,
    width: 42,
    label: " ",
    color: color(255, 0, 0),
});

var scene2redSquare5 = new Square({
    x: 259,
    y: 150,
    width: 42,
    label: "1",
    color: color(186, 186, 186),
    onClick: function() {
        if (currentColor === color(255, 0, 0)) {
        draw = function() {
            noStroke();
           scene2coloredredSquare5.draw(); 
        };
        }
    }
    
});

var scene2coloredredSquare6 = new Square({
    x: 259,
    y: 190,
    width: 42,
    label: " ",
    color: color(255, 0, 0),
});

var scene2redSquare6 = new Square({
    x: 259,
    y: 190,
    width: 42,
    label: "1",
    color: color(186, 186, 186),
    onClick: function() {
        if (currentColor === color(255, 0, 0)) {
        draw = function() {
            noStroke();
           scene2coloredredSquare6.draw(); 
        };
        }
    }
    
});

var scene2coloredredSquare7 = new Square({
    x: 133,
    y: 230,
    width: 42,
    label: " ",
    color: color(255, 0, 0),
});

var scene2redSquare7 = new Square({
    x: 133,
    y: 230,
    width: 42,
    label: "1",
    color: color(186,186,186),
    onClick: function() {
        if (currentColor === color(255, 0, 0)) {
        draw = function() {
            noStroke();
           scene2coloredredSquare7.draw(); 
        };
        }
    }
});

var scene2coloredredSquare8 = new Square({
    x: 175,
    y: 270,
    width: 42,
    label: " ",
    color: color(255, 0, 0),
});

var scene2redSquare8 = new Square({
    x: 175,
    y: 270,
    width: 42,
    label: "1",
    color: color(186,186,186),
    onClick: function() {
        if (currentColor === color(255, 0, 0)) {
        draw = function() {
            noStroke();
           scene2coloredredSquare8.draw(); 
        };
        }
    }
});

var scene2coloredredSquare9 = new Square({
    x: 217,
    y: 230,
    width: 42,
    label: " ",
    color: color(255, 0, 0),
});

var scene2redSquare9 = new Square({
    x: 217,
    y: 230,
    width: 42,
    label: "1",
    color: color(186,186,186),
    onClick: function() {
        if (currentColor === color(255, 0, 0)) {
        draw = function() {
            noStroke();
           scene2coloredredSquare9.draw(); 
        };
        }
    }
});

//startbutton for splash screen
var startButton = function(config) {
    this.x = config.x || 0;
    this.y = config.y || 0;
    this.width = config.width || 150;
    this.height = config.height || 50;
    this.label = config.label || "Click";
    this.onClick = config.onClick || function() {};
};

startButton.prototype.draw = function() {
    fill(255, 255, 255);
    rect(this.x, this.y, this.width, this.height, 5);
    fill(0, 0, 0);
    textSize(19);
    textAlign(LEFT, TOP);
    text(this.label, this.x+10, this.y+this.height/4);
};

startButton.prototype.isMouseInside = function() {
    return mouseX > this.x &&
           mouseX < (this.x + this.width) &&
           mouseY > this.y &&
           mouseY < (this.y + this.height);
};

startButton.prototype.handleMouseClick = function() {
    if (this.isMouseInside()) {
        this.onClick();
    }
}; 

var StartButton = new startButton({
    x: 145,
    y: 250,
    width: 113,
    label: "Start Game",
    onClick: function() {
        currentScene = 2;
    }
});

//splash screen 
var drawScene1 = function() {
    background(155, 203, 235);
    stroke(0, 0, 0);
    fill(255, 255, 255);
    textSize(39);
    text("Color By Number", 55, height/3);
    StartButton.draw();
    drawBitmoji(291,276,50);
    drawKelly(292,266,50);
}; 

var NextButton = new startButton({
    x: 300,
    y: 20,
    width: 80,
    height: 30,
    label: "  Next",
    onClick: function() {
        currentScene = 3;
    }
});

var NextButton2 = new startButton({
    x: 300,
    y: 340,
    width: 80,
    height: 30,
    label: "  Next",
    onClick: function() {
        currentScene = 4;
    }
});

//buttons
var Button = function(config) {
    this.x = config.x || 0;
    this.y = config.y || 0;
    this.width = config.width || 150;
    this.height = config.height || 50;
    this.label = config.label || "Click";
    this.color = config.color || fill(255, 255, 255);
    this.onClick = config.onClick || function() {};
};

Button.prototype.draw = function() {
    fill(this.color);
    ellipse(this.x, this.y, this.width, this.height);
    fill(0, 0, 0);
    textSize(19);
    textAlign(LEFT, TOP);
    text(this.label, this.x - 5, this.y+this.height/4 - 23);
};

Button.prototype.isMouseInside = function() {
    return mouseX > this.x &&
           mouseX < (this.x + this.width) &&
           mouseY > this.y &&
           mouseY < (this.y + this.height);
};

Button.prototype.handleMouseClick = function() {
    if (this.isMouseInside()) {
        this.onClick();
    }
};

//color buttons
var Button1 = new Button({
    x: 50,
    y: 354,
    width: 50,
    label: "1",
    color: color(255, 255, 0),
    onClick: function() {
        currentColor = color(255, 255, 0);
        draw = function() {
            fill(currentColor);
            rect(120,21,15,15);
        };
    }
});

var Button2 = new Button({
    x: 115,
    y: 354,
    width: 50,
    label: "2",
    color: color(255, 255, 255),
    onClick: function() {
        currentColor = color(255, 255, 255);
        draw = function() {
            fill(currentColor);
            rect(120,21,15,15);
        };
    }
});

var Button3 = new Button({
    x: 180,
    y: 354,
    width: 50,
    label: "3",
    color: color(43, 171, 60),
    onClick: function() {
        currentColor = color(43, 171, 60);
        draw = function() {
            fill(currentColor);
            rect(120,21,15,15);
        };
    }
});

var Scene2Button1 = new Button({
    x: 50,
    y: 354,
    width: 50,
    label: "1",
    color: color(255, 0, 0),
    onClick: function() {
        currentColor = color(255, 0, 0);
        draw = function() {
            fill(currentColor);
            rect(120,21,15,15);
        };
    }
});

var Scene2Button2 = new Button({
    x: 115,
    y: 354,
    width: 50,
    label: "2",
    color: color(255, 18, 144),
    onClick: function() {
        currentColor = color(255, 18, 144);
        draw = function() {
            fill(currentColor);
            rect(120,21,15,15);
        };
    }
});

var Scene2Button3 = new Button({
    x: 180,
    y: 354,
    width: 50,
    label: "3",
    color: color(255, 97, 202),
    onClick: function() {
        currentColor = color(255, 97, 202);
        draw = function() {
            fill(currentColor);
            rect(120,21,15,15);
        };
    }
});

var Scene3Button1 = new Button({
    x: 50,
    y: 354,
    width: 50,
    label: "1",
    color: color(51, 51, 51),
    onClick: function() {
        currentColor = color(51, 51, 51);
        draw = function() {
            fill(currentColor);
            rect(120,21,15,15);
        };
    }
});

var Scene3Button2 = new Button({
    x: 115,
    y: 354,
    width: 50,
    label: "2",
    color: color(180, 222, 202),
    onClick: function() {
        currentColor = color(180, 222, 202);
        draw = function() {
            fill(currentColor);
            rect(120,21,15,15);
        };
    }
});

var Scene3Button3 = new Button({
    x: 180,
    y: 354,
    width: 50,
    label: "3",
    color: color(168, 203, 255),
    onClick: function() {
        currentColor = color(168, 203, 255);
        draw = function() {
            fill(currentColor);
            rect(120,21,15,15);
        };
    }
});

var drawScene2 = function() {
    background(191, 228, 255);
    fill(0, 0, 0);
    textSize(25);
    text("Color the Flower", 110,53);
    textSize(15);
    text("Current Color: ",20,20);
    stroke(0, 0, 0);
    fill(currentColor);
    rect(120,21,15,15);
    NextButton.draw();
    color1Square1.draw();
    color2Square1.draw();
    color2Square2.draw();
    color2Square3.draw();
    color2Square4.draw();
    color3Square1.draw();
    color3Square2.draw();
    color3Square3.draw();
    Button1.draw();
    Button2.draw();
    Button3.draw();
    
};

var drawScene3 = function() {
    background(191, 228, 255);
    fill(0, 0, 0);
    textSize(25);
    text("Color the Heart", 110,53);
    textSize(15);
    text("Current Color: ",20,20);
    stroke(0, 0, 0);
    fill(currentColor);
    rect(120,21,15,15);
    NextButton2.draw();
    Scene2Button1.draw();
    Scene2Button2.draw();
    Scene2Button3.draw();
    scene2lightpinkSquare1.draw();
    scene2pinkSquare1.draw();
    scene2pinkSquare2.draw();
    scene2pinkSquare3.draw();
    scene2pinkSquare4.draw();
    scene2pinkSquare5.draw();
    scene2pinkSquare6.draw();
    scene2redSquare1.draw();
    scene2redSquare2.draw();
    scene2redSquare3.draw();
    scene2redSquare4.draw();
    scene2redSquare5.draw();
    scene2redSquare6.draw();
    scene2redSquare7.draw();
    scene2redSquare8.draw();
    scene2redSquare9.draw();
};

var drawScene4 = function() {
    background(191, 228, 255);
    fill(0, 0, 0);
    textSize(25);
    text("Color the Car", 110,53);
    textSize(15);
    text("Current Color: ",20,20);
    stroke(0, 0, 0);
    fill(currentColor);
    rect(120,21,15,15);
    Scene3Button1.draw();
    Scene3Button2.draw();
    Scene3Button3.draw();
};

mouseClicked = function() {
    if (currentScene === 1) {
        StartButton.handleMouseClick();
    }
    if (currentScene === 2) {
        NextButton.handleMouseClick();
        color1Square1.handleMouseClick();
        color2Square1.handleMouseClick();
        color2Square2.handleMouseClick();
        color2Square3.handleMouseClick();
        color2Square4.handleMouseClick();
        color3Square1.handleMouseClick();
        color3Square2.handleMouseClick();
        color3Square3.handleMouseClick();
        Button1.handleMouseClick();
        Button2.handleMouseClick();
        Button3.handleMouseClick();
    }
    if (currentScene === 3) {
        Scene2Button1.handleMouseClick();
        Scene2Button2.handleMouseClick();
        Scene2Button3.handleMouseClick();
        NextButton2.handleMouseClick();
        scene2lightpinkSquare1.handleMouseClick();
        scene2pinkSquare1.handleMouseClick();
        scene2pinkSquare2.handleMouseClick();
        scene2pinkSquare3.handleMouseClick();
        scene2pinkSquare4.handleMouseClick();
        scene2pinkSquare5.handleMouseClick();
        scene2pinkSquare6.handleMouseClick();
        scene2redSquare1.handleMouseClick();
        scene2redSquare2.handleMouseClick();
        scene2redSquare3.handleMouseClick();
        scene2redSquare4.handleMouseClick();
        scene2redSquare5.handleMouseClick();
        scene2redSquare6.handleMouseClick();
        scene2redSquare7.handleMouseClick();
        scene2redSquare8.handleMouseClick();
        scene2redSquare9.handleMouseClick();
    }
    if (currentScene === 4) {
        Scene3Button1.handleMouseClick();
        Scene3Button2.handleMouseClick();
        Scene3Button3.handleMouseClick();
    }

    
    
};

//draws different levels
draw = function() {
    if (currentScene === 1) {
        drawScene1();
    }
    if (currentScene === 2) {
        drawScene2();
    }
    if (currentScene === 3) {
        drawScene3();
    }
    if (currentScene === 4) {
        drawScene4();
    }
    
};
